#!/usr/bin/env
import 'zx/globals'

export const red = (text) => console.log(chalk.red(text))
export const yellow = (text) => console.log(chalk.yellow(text))
export const blue = (text) => console.log(chalk.blue(text))
export const green = (text) => console.log(chalk.green(text))
